# Curated domain context

## Approved outcome

The user approved preserving `raw/` as an unchanged source archive and creating five concise Russian-language documents in `domain/`: an index, problem and scope, model and rules, data contract, and acceptance scenarios. The purpose is selective context loading without repeated contractor descriptions or promotional material.

## Boundaries

This is source-document curation, not product implementation. Preserve all source requirements, evaluation weights and material dataset limitations. Distinguish source facts, measured dataset properties, example-check assumptions and unresolved product decisions. Keep CSV as machine-readable input without copying its records into documentation. OpenSpec remains authoritative for active change requirements and task state; architecture remains in `architecture/`.

## Verification

Compare the brief with the resulting documents, reconcile CSV and preview IDs, compute statistics and example candidate sets from CSV, check local links, and confirm source hashes are unchanged. No new infrastructure, dependencies, accounts or application execution are required. Preserve existing staged work; do not publish it as part of this documentation task.
