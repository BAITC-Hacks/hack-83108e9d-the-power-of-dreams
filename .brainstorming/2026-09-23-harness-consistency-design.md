# Harness consistency

Bounded tooling/documentation update approved by the user on 2026-09-23: apply
the preceding harness audit recommendations. The approved outcome is to preserve
authorization across workflow skills, distinguish implementation stages from final
delivery, verify harness availability before delegation, pin immutable contract
packages, and identify the owner of shared integration resources across Codex tasks.

Use the configured MCP or a preinstalled CLI for read-only documentation research.
Allow narrowly evidenced critical incidental review findings without a general
audit. Keep AGENTS.md concise, clarify explicit change switching, and require
periodic organizer reporting only when the brief requires it.

Keep generated OpenSpec skills unchanged; maintain project adaptations in
AGENTS.md, OpenSpec configuration, project role prompts, and the delivery skill.
This work adds no product requirements, application dependencies, external accounts,
or services. A local ignored ownership marker is used only during shared writes.
The existing publication hold and unrelated local preparation remain intact.

Verify configuration parsing, delivery-skill validation, OpenSpec instruction
delivery/validation, consistency of changed references, and unchanged generated
skills. Record observed results and limitations in docs/tooling.md. Static checks
do not establish live subagent isolation or concurrent ownership behavior.

This file records the approved outcome. Current workflow rules remain in AGENTS.md
and its referenced configuration; current feature state remains in OpenSpec.
