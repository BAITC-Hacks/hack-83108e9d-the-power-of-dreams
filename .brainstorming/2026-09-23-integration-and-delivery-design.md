# Approved P07 integration and delivery outcome

The user authorized proposal 07 through all OpenSpec stages on 2026-09-23, with role-specific subagents, simple implementation, and explicit ownership. This is bounded delivery work on the published P05/P06 application, not a new product or redesign.

Use the current application, public v1 contract and existing acceptance runners. Add only the missing final three-request live browser timing/quality evidence and concise provenance/demo documentation. Reuse unchanged, revision-bound module and controlled-browser evidence. Verify a clean committed checkout following the final README, then integrate and publish under the shared reservation, synchronize specs and archive after actual completion.

The coordinator owns shared state, final checks, README reconciliation, Git integration and publication. A bounded apply worker owns material provenance; a read-only review worker checks evidence coverage and final acceptance. No new dependencies, provider, GPU, database, infrastructure or product behavior. The independently authorized Docker change may advance main; preserve it and its documented optional launch method. Wait for its owner to release shared resources rather than competing for them.

Live checks use the existing private OpenAI configuration through the server loader, incur three normal billable requests, and record public explanations only. No credential changes, organizer submission, deployment or external messages beyond repository task coordination are authorized by this scope. Missing mandatory evidence remains a blocker, not an implied pass.
