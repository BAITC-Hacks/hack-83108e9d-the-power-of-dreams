## 1. Presentation

- [ ] 1.1 Implement compact identity and separate explanation-led cards; verify real primary and rare results, complete explanations and first desktop viewport.
- [ ] 1.2 Implement passive complete snapshot labels and consistent initial/mobile states; verify both empty outcomes, date changes, pending/draft/errors, focus and 375/390/1280 px layouts.

## 2. Acceptance and publication

- [ ] 2.1 Run typecheck, existing tests, production build and real/controlled browser checks; verify clean README installation/build/start/primary and record actual evidence/limitations with project-delivery.
- [ ] 2.2 Review scoped diff, publish feature, acquire integration ownership, verify combined candidate, publish main and record exact revisions; preserve other work.

## Stage table

| Task | Stage | Evidence / revision | Remaining checks | Hold / blocker | Next action / owner |
| --- | --- | --- | --- | --- | --- |
| 1.1 | implemented | Real primary/rare and live renderer passed; screenshots reviewed | Integrated candidate | None | Coordinator integrate |
| 1.2 | implemented | Real and controlled browser checks at 375/390/1280 passed | Integrated candidate | None | Coordinator integrate |
| 2.1 | implementing | Typecheck/build/48 tests, real/control/service/live passed | Clean candidate installation/launch | None | Coordinator verify |
| 2.2 | planned | Scoped source review; synchronized specs valid | Commit / candidate / publication | Other task owns integration | Coordinator wait for release |

## Assignment and readiness

Owner: coordinator 01a0ce40-6c41-7a51-a4c4-58418001f027; no subagents. Active change: join-city-visual-refresh. Prior contractor-ui-upgrade is delivered; unrelated active changes untouched.

Worktree D:/Alem/hack-83108e9d-the-power-of-dreams-wt-ui-upgrade; branch codex/ui-upgrade; base 1a9a02353012a01ec8770acc55061bc145fc261a; origin https://github.com/BAITC-Hacks/hack-83108e9d-the-power-of-dreams.git; target main. Remote main checked at the same revision. Initial unrelated .gitignore modification is preserved/excluded. Approved brainstorming file initially untracked; include only that referenced outcome in scoped delivery.

Allowed writes: four presentation files in design, focused scripts/frontend/browser.mjs and races.mjs acceptance additions, this change and synchronized contractor-frontend-flow spec, architecture index, source-provenance record if needed. No backend, data, contracts, dependencies or secrets edits. User explicitly requested implementation; standing feature commit/push/main authorization applies. Historical planning-only wording in the outcome does not override this request.

Harness: AGENTS.md, .codex/config.toml, OpenSpec configuration and required skills exist at base 1a9a023; runtime restrictions remain authoritative. No delegation or claim of runtime isolation. Direct reads used for bounded JSX/CSS changes; no symbol relationship uncertainty requiring index rebuild.

Contract v1: D:/Alem/hack-83108e9d-the-power-of-dreams/.shared/specs/contractor-selection/versions/v1, pinned package e424a13fec851d7f9f0f4e076f70649e716336f7, backend 4ade2fa4022d633e9d96b50188be4cee4fb99539. Verify version snapshot before implementation. Reconcile any later main/backend changes before integration.

Checks: npm run typecheck, npm test, npm run build; scripts/frontend/browser.mjs (real catalogue primary/rare/empty/date/keyboard), races.mjs (controlled concurrency/errors/modes/focus), minimal visual extensions. Primary expected IDs HK-88430/HK-29829/HK-27222, five eligible. Ports 3134 preview and 3135 clean candidate after availability checks; outputs in test-results/join-city-refresh. Explicit empty OPENAI_API_KEY for real catalogue fallback; optional live check recorded separately. Screenshot widths 375/390/1280, desktop height 900. Clean candidate follows README without .env or existing build/dependencies, fresh browser context. Preserve historical backend/live-quality evidence without mislabelling it current.

Shared reservation: none acquired; acquire exclusively before integration/main writes, record owner/resources/time here. Stop affected work for breaking contract, conflicting writer or missing package. Git network read required escalation because sandbox Windows credential access failed; escalated read succeeded, no approval rejection.

## Evidence

Planning: approved outcome transferred into standard spec-driven proposal/design/spec/tasks; no re-approval gate or change to agreed product scope.

Local acceptance on baseline 1a9a023 plus scoped presentation changes: typecheck, production build and all 48 tests passed (zero skips). Contract v1 six files matched pinned Git blob hashes before edits; backend ancestor confirmed. Existing real browser suite passed primary IDs/count, rare, both empty outcomes, optionals, date changes, real HTTP400 and explicit keyboard/mobile navigation. Controlled suite passed duplicate/supersession/reset/stale errors/malformed/500/mixed/options plus snapshot isolation, independent missing optional labels, unknown/long category, maximum safe-integer price, complete text and 375/390/1280 overflow. Mobile focused duration field is above the fixed bar with outline clearance; reduced-motion and focus checks passed. Evidence: test-results/join-city-refresh including initial, primary, width/result/long screenshots and mobile-focus.png. Desktop first full explanation remains in 1280×900; screenshots visually reviewed.

Contrast computed from actual colors: white/blue button 4.59:1, field boundary/white 3.18:1, supporting text/canvas 5.09:1, main text/white 11.17:1. No new external asset/font requests or dependencies. Original local vector provenance recorded in THIRD_PARTY.md using project-delivery; existing README launch instructions remain applicable. Main frontend spec synchronized via openspec-sync-specs; all eight current specs and this change validate strictly.

Real isolated catalogue failure/retained-results/retry checks passed on port3136 (test-results/p06-service-6pIOyk). One live production submission on3137 passed openai_evidence and expected IDs, full rendered explanations matched public response (test-results/p06-live-sS40Xw); screenshot reviewed. Existing secret loader read primary private .env without logging credentials. Isolated services stopped themselves. Historical backend quality/timing evidence is not claimed as rerun.

Coordination: task01a0ce1a-086f-7772-9104-a72718f35156 owns shared integration/main/v2 marker for contractor-brief-matching. It reported accepted sourcea4c82b6 and packagev2ce79615 and will supply published main. This coordinator acknowledged and leaves shared resources untouched. On reconciliation preserve BriefEditor after optional fields, BriefAdvice after explanation, explicit review/confirm and exports. Integrate accepted new main into this feature before final combined checks; never overwrite another owner's staged README.
